"use client";
import { useState } from "react";
export default function ContactForm(){const [state,setState]=useState('idle');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('sending');const fd=new FormData(e.currentTarget);const data=Object.fromEntries(fd.entries());const r=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});setState(r.ok?'sent':'error')}return <form onSubmit={submit} className="card dark-form" style={{padding:26,display:'grid',gap:16}}><div className="grid-2"><div className="form-field"><label>Name</label><input name="name" required/></div><div className="form-field"><label>Company</label><input name="company"/></div></div><div className="grid-2"><div className="form-field"><label>Email</label><input type="email" name="email" required/></div><div className="form-field"><label>Phone</label><input name="phone"/></div></div><div className="form-field"><label>What do you want to discuss?</label><select name="topic"><option>General opportunity</option><option>Measurement</option><option>Transportation & logistics</option><option>Marketing & market access</option><option>AI & automation</option><option>Contract Intelligence</option><option>Operations consulting</option></select></div><div className="form-field"><label>Tell us what is happening</label><textarea name="message" rows={7} required/></div><button className="btn btn-primary" disabled={state==='sending'}>{state==='sending'?'Sending...':'Send to Copperhead'}</button>{state==='sent'&&<div style={{color:'#7fdda5'}}>Message received. Copperhead will follow up.</div>}{state==='error'&&<div style={{color:'#ff9d8f'}}>The form could not send. Email info@copperheadoil.com directly.</div>}</form>}
